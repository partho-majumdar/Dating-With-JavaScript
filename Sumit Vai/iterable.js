// object ke iterate kora jay na

const names = {
  firstName: "John",
  lastName: "Doe",
};

// array ke iterate kora jay for...of loop use kora hoy

const numbers = [1, 2, 3, 4, 5, 6];

// string o iterable

let a = "Bangladesh";

// set k o iterate kora jay

const set = new Set([1, 3, 4, 5, 6]);

console.log(set);

console.log(new Set());
